import React from "react";
import ReactDOM from "react-dom/client";
import "./style.scss";
import Formapp from "./components/formapp";

const root=ReactDOM.createRoot(document.getElementById("form-app"))
root.render(
  

  <Formapp />
);
